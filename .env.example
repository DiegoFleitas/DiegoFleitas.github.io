# Required GA4 Measurement ID.
# Set this in local env files and in CI/CD build variables.
VITE_GA_ID=G-XXXXXXXXXX

# Resume PDF: add `Diego-Fleitas-Resume.pdf` under `public/` (same-origin; deployed with GitHub Pages).

# In GA4 Admin → Custom definitions, register event-scoped dimensions you need for reports
# (e.g. referrer_host, page_language, utm_*, file_name, link_url for file_download).
